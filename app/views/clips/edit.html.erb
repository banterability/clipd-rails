<%= error_messages_for :clip %>

<% form_for(@clip) do |f| %>
 
  <%= render :partial => "form", :locals => {:f => f} %>
 
  <p><%= submit_tag "Update" %></p>
  
<% end %>

<p><%= link_to '&laquo; Back to clip', clip_path(@clip) %></p>