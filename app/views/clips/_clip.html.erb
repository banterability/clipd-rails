<div class="clip-body">
	<div class="clip-content">
		<span class="bigQuote">&#8220;</span>
					<%=h clip.selection %>
	</div>
	<div class="clip-meta">
		<span class="clip-actions">
				<%= link_to image_tag('icons/link.png'), clip %>
				<%= link_to image_tag('icons/delete.png'), clip, :confirm => 'Are you sure?', :method => :delete %>
		</span>
		<span class="clip-info">
				<% if clip.sourceUri == nil -%>
					<%= h(wb_truncate(clip.sourceTitle)) %>
				<% else -%>
					From <%= link_to h(wb_truncate(clip.sourceTitle)), clip.sourceUri -%>
				<% end -%>
				 <%= timeago clip.created_at %>
		</span>
		<br style="clear:both" />
	</div>
</div>